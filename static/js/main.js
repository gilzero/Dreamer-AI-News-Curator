// Main entry point for the Dreamer AI News Curator
import { initializeApp } from './init.js';

// Initialize the application when the DOM is loaded
document.addEventListener('DOMContentLoaded', initializeApp);